@model IEnumerable<MvcMovie.Models.Book>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.SciFiVotes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FantasyVotes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Author)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SciFiVotes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FantasyVotes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID }) |||
            @Html.ActionLink("RateSciFi", "RateSciFi", new { id=item.ID }) |
            @Html.ActionLink("RateFantasy", "RateFantasy", new { id=item.ID })
        </td>
    </tr>
}

</table>
